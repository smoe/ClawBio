# Per-skill detail outputs from clawbio-bench can be large (hundreds of MB).
# Only the aggregate_report.json under latest/ is committed; everything else
# is regenerated by the workflow on demand.
latest/*/
!latest/aggregate_report.json
